# V2 Scripts

- `generate_reference_goldens.py` regenerates compact V1 RGBA8 fixtures and refuses to write a
  golden result that differs from the V1 production renderer.

Run migration scripts from the repository root with `PYTHONDONTWRITEBYTECODE=1`.
